export type ProfileProps = {
    username: string;
    achievementlist: string; // change to achievements part, done as seperate component
    friendlist: string; // change to friendlist part, done as seperate component
}